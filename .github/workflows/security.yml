name: Security Scanning

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  schedule:
    # Run security scans daily at 2 AM UTC
    - cron: "0 2 * * *"
  workflow_dispatch:

permissions:
  security-events: write
  contents: read

jobs:
  trivy-scan:
    name: Trivy Security Scanning
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: "fs"
          scan-ref: "."
          format: "sarif"
          output: "trivy-results.sarif"

      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: "trivy-results.sarif"
        if: ${{ always() && (github.event.pull_request == null || github.event.pull_request.head.repo.full_name == github.repository) }}

  secret-scan:
    name: Secret Detection
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Run Secret Scanner (pull requests - compare base & head)
        if: ${{ github.event_name == 'pull_request' }}
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.pull_request.base.sha }}
          head: ${{ github.event.pull_request.head.sha }}

      - name: Run Secret Scanner (branch/scheduled runs)
        if: ${{ github.event_name != 'pull_request' }}
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./

  # Uncomment and configure for specific container image scanning
  # trivy-image:
  #   name: Trivy Image Scanning
  #   runs-on: ubuntu-latest
  #
  #   steps:
  #     - name: Run Trivy container image scanner
  #       uses: aquasecurity/trivy-action@master
  #       with:
  #         image-ref: "your-registry/your-image:latest"
  #         format: "sarif"
  #         output: "trivy-image-results.sarif"
  #
  #     - name: Upload results to GitHub Security
  #       uses: github/codeql-action/upload-sarif@v4
  #       with:
  #         sarif_file: "trivy-image-results.sarif"
  #       if: always()
